# ILMATRIX Environment Configuration

# Server Configuration
PORT=8787
HOST=localhost
NODE_ENV=development

# Groq AI Configuration
GROQ_API_KEY=your_groq_api_key_here
GROQ_MODEL=meta-llama/llama-4-maverick-17b-128e-instruct
GROQ_CONCURRENCY=4
GROQ_TIMEOUT_MS=45000

# Material Configuration
# Max characters from uploaded materials to include in prompts (to avoid truncating multi-file content)
# Default in code is 100000; override here if needed (e.g., 200000). Keep within your provider token limits.
MATERIAL_CLAMP=200000
MATERIAL_TTL_MINUTES=60

# Rate Limiting
RATE_LIMIT_MAX=120

# File Processing
PDF_MAX_PAGES=200

# Database Configuration (Railway PostgreSQL)
# Use either DATABASE_URL (recommended) or individual PG components

# Railway Database URL (primary method)
DATABASE_URL=postgresql://username:password@host:port/database
# DATABASE_PUBLIC_URL=postgresql://username:password@host:port/database

# Individual PostgreSQL components (fallback)
# PGHOST=localhost
# PGPORT=5432
# PGDATABASE=ilmatrix
# PGUSER=postgres
# PGPASSWORD=password

# Development only - remove in production
# DISABLE_AUTH=true